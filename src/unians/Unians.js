import React, { Component } from 'react';

import { AppContainer } from './Unians.styles';
import { ReduxContainer } from '../components';

class Unians extends Component {
  
  render() {
    
    return (
      <AppContainer>
        
      </AppContainer>
    );
  }
}

export default ReduxContainer({
  selectors: {
    
  },
  formatSelectors: {
    
  },
  actions: {
    
  }
})(Unians);

